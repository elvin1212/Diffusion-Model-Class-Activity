version: '3.8'

services:
  cifar10-app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app/app
      - ./diffusion:/app/diffusion
      - ./EBM:/app/EBM
    environment:
      - UVICORN_HOST=0.0.0.0
      - UVICORN_PORT=8000